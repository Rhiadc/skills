# Definition of done — Go

> Generated by golang-development. Lenses: quality · tests · security · architecture · concurrency · evidence.

## Meta

| Field | Value |
|-------|-------|
| Scope | _paths / feature / bugfix_ |
| Module / Go version | _from go.mod_ |
| Mode | full / lite |
| Date | _YYYY-MM-DD_ |
| Evidence | _go vet / go test -race / govulncheck: ran \| skipped (why)_ |
| Status | _done \| blocked (why)_ |

## Summary

- _What was built or fixed_
- _Packages touched_
- _Key design choices (ports, concurrency, security)_

---

## Quality

_Idioms, types, errors, stdlib — 100 Go Mistakes + Cheney. Not test design — see Tests._

<!-- Gaps fixed or "No gaps" + checklist. Repeat ### Qn if documenting remaining nits. -->

### Checks

- [ ] Early returns; no harmful shadowing (#1–#2)
- [ ] Errors wrapped/compared with `%w` / `Is`/`As` as needed (#48–#54)
- [ ] Resources closed; HTTP error paths return (#79–#80)
- [ ] No invented APIs; matches module style

---

## Tests

_Coverage of the change, table-driven/subtests, hermeticity, -race, fuzz when relevant._

### Checks

- [ ] Test would fail if the change were reverted
- [ ] Happy path + ≥1 error/boundary case
- [ ] Table-driven + `t.Run` when ≥2 cases (#85)
- [ ] No Sleep-as-sync; clock/IO seams where needed (#86–#88)
- [ ] Concurrent packages exercised under `-race` (#83)

---

## Security

### Checks

- [ ] Authn/authz on new handlers; no package-global identity
- [ ] Parameterized SQL / safe exec / path confinement
- [ ] HTTP server/client timeouts + body limits (#81)
- [ ] No secrets in code/logs; `crypto/rand` for tokens
- [ ] `govulncheck` considered for new deps

---

## Architecture

_Skip deep checklist in lite mode if lens did not apply — note "skipped (lite)"._

### Checks

- [ ] Dependency direction preserved (domain free of HTTP/SQL)
- [ ] Interfaces at consumer; no premature `utils` packages
- [ ] Composition at `cmd` / edge
- [ ] Domain errors mapped at adapters

---

## Concurrency

_Skip if lite and no goroutines/shared state._

### Checks

- [ ] No unsynchronized shared mutable state
- [ ] Every `go` has a stop plan (ctx / WaitGroup / errgroup)
- [ ] Context: first param; `defer cancel()`; no request ctx for post-handler work (#60–#62)
- [ ] `WaitGroup.Add` before `go`; no copied `sync` types (#71, #74)

---

## Gaps remaining (optional)

| ID | Severity | Lens | Location | Fix / defer reason |
|----|----------|------|----------|--------------------|
| | | | | |

### Out of scope

- _Explicitly not done_

---

## Ship rule (reference)

- Any **Must** (critical/high) open → not done
- Open **Should** (medium) → default fix before handoff
- Only nits left → done (optional polish)
