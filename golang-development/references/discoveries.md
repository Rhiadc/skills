# Discoveries

Append-only log of reusable Go learnings from real work. **Do not preload** this file at the start of a task — open it only when logging, deduplicating, or promoting.

Proposals happen **after the job finishes**, with finding + why it matters + importance (see SKILL.md). Status: `logged` → `promoted`.

---

<!-- Newest entries at the top. Use the template in SKILL.md → Discoveries. -->
